.approval-content,
.failure-content,
.success-content {
	display: flex;
	height: 160px;
	justify-content: center;
	align-items: center;
	gap: var(--space-4);
}

.success-data {
	display: flex;
	flex-direction: column;
	align-items: center;
	gap: var(--space-3);
}
