.chain-selector {
	display: flex;
	flex-direction: row;
	align-items: center;
	justify-content: center;
	gap: var(--space-2);
	padding: var(--space-1) var(--space-3) var(--space-1) var(--space-2);
	border-radius: var(--space-3);
	cursor: pointer;
}

.chain-selector:focus {
	box-shadow:
		0px 0px 0px 2px var(--color-gray-25),
		0px 0px 0px 4px var(--color-accent-500);
}

.chain-selector:hover {
	background-color: var(--color-gray-100);
}

.chain-selector:active {
	background-color: var(--color-gray-200);
}

.chain-selector__pointer {
	font-size: 0.875rem;
	font-weight: 400;
	line-height: 18px;
}

.chain-selector__token,
.chain-selector__chain {
	display: flex;
	flex-direction: row;
	align-items: center;
	justify-content: center;
	gap: var(--space-1);
}

.chain-selector__token-name,
.chain-selector__chain-name {
	font-size: 1rem;
	font-weight: 400;
	line-height: 18px;
	color: var(--color-gray-700);
	align-items: center;
}
